{
  "name": "@hongling/agy-plugin-cc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Use Antigravity CLI from Claude Code.",
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
